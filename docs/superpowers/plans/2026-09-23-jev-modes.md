# Jev 意图与回复模式实施计划

已获用户“按此方案开发”授权，由当前代理原位实施，不提交既有改动。

目标：完整判断、概率展示、六种回复模式、Skill 调查结果复用和当前会话内分模式缓存。

- [x] 判断与评估：保留工作场景四题，补充字面含义、对方需求、实质回复条件、紧张状态；风险改为 0–9 Score。保留概率与置信度；候选逐条评估事实依据、模式贴合和升级风险。通过 Jev 边界测试验证缺字段、未知、分布与排序映射。
- [x] 模式与调查：专业/友善/简短/坚定/怼人/幽默；设置默认模式。生成器输出三条回复及有来源的调查摘要。切模式只调用无工具改写，并将摘要提供给排序。
- [x] 会话缓存：基础分析 ID 关联上下文快照、Skill、配置版本；当前会话下复用判断与模式结果。异步进度绑定请求及模式；切走清空，排序失败可独立重试。服务测试验证缓存命中、模式切换无 Skill、跨会话与过期结果。
- [x] 界面：分析前模式与 Skill 选择；概览/完整判断/三条带概率和评估的候选；模式切换复用缓存，复制/填入；识别已公开的群成员身份，未知身份保持分离。
- [x] 验证交付：合成界面流程、完整 npm test、独立审阅、打包与包内测试。不得使用真实聊天或用户凭证进行外部测试。

接口：start 快照新增 modeId/baseRequestId；生成器保留 replies 数组返回，新增 onEvidence 回调；分析结果包含 analysisId/modeId/evidence；Jev rank 接收 modeId/evidence。默认模式 professional。

重点：低置信度不得标为确定事实；概率不是成功率；切模式不能重跑工具；群成员不能合并为一个人；迟到结果不能覆盖新模式。

实施与验证：所有上述功能已实现。35 项 Node 测试、合成界面流程及 14 项既有功能回归通过。独立审阅发现“改写失败/取消后无法返回旧结果”和“重试成功后取到早期失败缓存”，均以新增用例先复现再修复；完整 npm test 通过。真实本地 Codex 运行合成算术 Skill 得到 323，返回 1 项带来源摘要，之后无工具改写返回 3 条候选；未使用真实聊天或业务工具。深浅色截图已检查。

实际群成员识别目前仅使用页面明确公开的 data-sender-id/name；缺失时为每条未知消息保留独立身份。没有以合成 DOM 测试声称已覆盖所有飞书群聊版本。当前上下文和调查摘要是分析时的快照，切模式不重新调查；明确重新分析才更新。

交付：/tmp/electron-lark-jev-modes-20260923/mac-arm64/Electron Lark.app 构建及 ad-hoc 签名通过，JEV_TEST_APP_ROOT 指向 app.asar/src 的合成界面测试通过。未替换 /Applications 中安装版，未重启用户当前运行的旧版。代码保留未提交。
