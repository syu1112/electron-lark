# SDD ledger — plan: docs/superpowers/plans/2026-09-23-jev-chat-assistant.md

执行方式：用户确认主代理直接实施，最后独立审阅。
Pre-flight: Task 1 runtime/public settings → Tasks 2–6，字段一致；Task 2 filter/judge/rank 和 Task 3 generateReplies → Task 4，字段一致；Task 4 progress → Task 5 IPC → Task 6 UI，字段一致。
Ruling: 在用户当前专用分支 codex/jev集成-0923 原位实施 — 已确认在当前任务直接实施，避免额外工作区切换；不动现有 .gitignore；代价是改动保留在当前工作区。
Ruling: 使用本文件记录进度，源码保留未提交供用户审阅 — 不引入会修改 .git/info/exclude 的辅助目录，也不自动提交用户未要求提交的改动；代价是需要保留工作区。
基线：既有 Electron 测试 12 通过 / 1 Enter 换行失败，已在设计中记录。

Task 1–4: completed. Settings store、Jev client、controlled Codex CLI、analysis service；21 Node tests pass。CLI schema copied to private temporary directory so native child can read it after asar packaging。Total snapshot text capped at 60,000 chars, with visible request to reduce N.
Task 5: completed. Main renderer isolated and Node disabled; settings use separate preload and sender checks; secrets stay in main process. Notification and shortcut regression migrated.
Task 6: implemented and synthetic integration passes. Actual DOM verified: .js-message-item[id], .richTextContainer, .message-self/.message-not-self, .messageAction__toolbar inside row, active [data-feed-active="true"] ancestor [data-feed-id], .chatMessages/.chatContainer and existing editor selector. Remaining actual-DOM check: quoted-message structure and final appearance; desktop became locked.
Task 7: in progress. Independent review found retry/cancel race, active duplicate clicks, missing quote extraction and characterData changes. First, second and fourth fixed; regression added for race. README updated.

Verification: full npm test passed on 2026-09-23: 21 Node tests, full synthetic pipeline/UI, 13 legacy checks. The legacy first-key test sampled before input dispatch completed; waiting for the single input's observable result (without resending) resolved it. Settings singleton regression test now waits for closed before reopening.
Verification: actual installed Codex 0.155.0-alpha.16 generated three schema-valid replies from one synthetic message using production invocation and current login; no real chat sent.
Verification: electron-builder --mac --arm64 --dir -c.directories.output=/tmp/electron-lark-jev-build-20260923 passed. asar contains new preloads, schema and assistant modules; no running application replaced. Jev live API remains untested without a configured key.

Final review follow-up: fixed cross-message rank retry overwriting active request, and cache restoration after inner-subtree rerender. Reviewer confirmed both fixes with no further important findings in that scope. Synthetic integration covers these and non-text messages making zero requests.
Final verification: 21 Node tests + full synthetic UI pipeline + 14 legacy/security checks passed; directory build passed; running the UI suite with JEV_TEST_APP_ROOT pointing inside packaged app.asar passed (including child CLI schema access).
Follow-up: self-chat synthetic quoted message verified actual .reply-header .referencePreviewTitle__message DOM. Direct-quote extraction is implemented with unique complete-text matching within selected candidates; duplicate/truncated quotes and N=0 do not expand context. Quote changes invalidate results. Full npm test passed again (21 Node tests, synthetic UI pipeline, 14 legacy/security checks); independent quote-delta review found no new blocking issues. Updated arm64 directory build and ad-hoc signing passed.
Live check: new build launched from /tmp/electron-lark-jev-build-20260923/mac-arm64/Electron Lark.app without replacing the installed app. Self-chat and synthetic quoted message are visible. Desktop locked before toolbar click, so final live positioning/scroll/narrow-window checks remain unverified. User will configure Key themselves; no live Jev API call was made. User will handle the previously disclosed accidental group message; do not retract or otherwise touch it.

Follow-up after user configured Key: actual pinned self-chat has no [data-feed-active="true"]. Elements inspection confirmed .feed-shortcut-list-inner .feed-item.active without an exposed chat ID. Added per-mounted-pinned-node WeakMap identity and class-change observation; ordinary feed IDs remain in use for regular chats. Regression first failed for missing toolbar buttons, then full npm test passed. Independent delta review found no confirmed blocking issue; same-node reuse for a different pinned chat is an unverified upstream lifecycle limitation.
Build /tmp/electron-lark-jev-pinned-20260923/mac-arm64/Electron Lark.app and ad-hoc signing passed. Live self-chat now displays the Jev button. For the synthetic test, contextLimit was temporarily changed from 10 to 0; restore it afterwards. Start page was set to the currently used tenant URL https://jtexpress.feishu.cn/next/messenger/ so configured-origin validation matches the chat page. First live synthetic request passed Jev judgment and reached local Codex generation.
Live synthetic pipeline completed: three ranked recommendations appeared beneath the selected self-chat message; clicking the first inserted its exact text into the composer with the "已追加到输入框，确认后再发送" status. No reply was sent. Test draft was cleared and contextLimit restored to 10, verified both in the settings UI and persisted config. The current running build is the pinned-chat fix under /tmp; /Applications remains unchanged.

2026-09-23 follow-up approved: system light/dark theme, session-only reopenable cache, configured local Skill selection with full command/tool execution, and Codex reasoning effort. Implemented in existing assistant/settings flow. Collapsing keeps the current result; reopening makes no model call; switching conversation clears frontend/backend state. Skill selection precedes model calls and passes only configured IDs to main. No-Skill calls remain controlled exec; selected Skills use the installed Codex app-server over stdio with extraRoots registration and enabled-catalog validation, native skill input, inherited tool configuration, ephemeral workspace, and per-request approval/user-input UI. No automatic chat send was added.
Verification: theme screenshots inspected at /tmp/jev-ui-preview/light.png and dark.png; tests cover reopen/no extra requests, system theme switching, skill picker/no early model request, trusted approval only, settings persistence and private-picker IPC denial. Native Codex synthetic Skill executed python3 to calculate 17*19, with completed command event/output 323 and three matching replies. No real conversation or business tool was used in this smoke test.
Independent review found a cancellation defect where a child ignoring SIGTERM could survive its parent. Added a real descendant-process regression (RED then GREEN), retained and awaited the process-group SIGKILL fallback before completing cleanup. Reviewer confirmed the fix and found no further blocking issue in the reviewed interaction scope.
Final follow-up verification: 28 backend tests, synthetic UI pipeline and 14 legacy/security checks passed. Built and ad-hoc signed /tmp/electron-lark-jev-skills-20260923/mac-arm64/Electron Lark.app. Packaged-source UI testing exposed a test input-dispatch race; the harness now waits for the trusted click to be delivered before changing message identity, and the packaged suite passed. No production code changed for this harness correction. Launched the new build after normally quitting the old temporary build; Jev settings visibly show Codex 思考强度 and 添加本地 SKILL. Left that settings page open, without changing the user's Key, Skill configuration or default effort. /Applications remains unchanged.
